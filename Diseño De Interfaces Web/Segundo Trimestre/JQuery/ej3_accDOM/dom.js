$(document).ready(function() {

    // Trabajo con le padre de un elemento dado - podemos cambiar su css
    // $('#tercera').parent().css({
    // 	background: '#1b3d82'
    // });

    // Todos los padres de uno dado
    // $('#tercera').parents();

    // Los hijos de un elemento
    // $('#interior').children().fadeOut(500);
    // $('#interior').children('#tercera').fadeOut(1000);

    // Encontrar un elemento concreto
    // $('#contenedor').find('div.caja1').slideUp();

    //Siblings - Eleemntos hermanos de uno seleccionado
    //$('#tercera').siblings().fadeOut(8000);

    // Trabajo con anteriores y posteriores a un elemento
    /*$('#tercera').prevAll().css({
        background: '#000'
    });*/

});