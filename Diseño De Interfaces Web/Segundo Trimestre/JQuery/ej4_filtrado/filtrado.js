$(document).ready(function() {

    // first() - Devuelve el primer elemento de varios seleccionados
    // $('.caja1').first().css({background: '#f9a03f'});

    // last() - Devuelve el ultimo de varios seleccionados
    // $('.caja1').last().css({background: '#f9a03f'});


    // eq() - Devuelve un elemento en base a un index.
    // $('.caja1').eq(2).css({background: '#f9a03f'});

    // filter() - Nos permite filtrar entre los elementos mediante un criterio.
    /* Ejemplo:
    		Selecciona todas las cajas, despues de esa seleccion solo selecciona la #segunda.
    */

    // $('.caja1').filter('#segunda').css({background: '#f9a03f'});

    // not() - Devuelve los elementos que no concuerdan con el criterio.
    //$('.caja1').not('#segunda').css({ background: '#79a83f' });




});